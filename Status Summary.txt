Status Summary

Done now (after you run the blocks):

pg_hba.conf cleaned; Postgres starts on 127.0.0.1:5432.

Risk Manager db_connect() bug bypassed; service restarts cleanly.

DB → user/db present; localhost password auth enabled.

(Optional) Port auto-switch tool provided.

Pending:

Confirm Postgres actually starts (no FATAL in logs).

Confirm psql SELECT 1 returns 1.

Confirm omv2-risk logs snapshots and enqueues controls on breach.

Open issues to watch:

If pg_hba.conf was hand-edited elsewhere, keep the fixed localhost rules.

Ensure /opt/omv2/db.env stays in sync if you change PGPORT.

a. Want me to push a cleaned-up omv2_risk_manager.py (full file) with the DSN bug removed and minor robustness?
b. Want a one-liner omv2-db-port-auto helper that permanently finds/sets the next free port and restarts everything?


